[pytest]
testpaths = backend/tests
markers =
    integration: multi-component tests that remain isolated from external services
    live: tests that require explicitly configured external services and are not part of pull-request CI
